# Inverted_Search
A C based inverted search system that indexes words from multiple text files using hash tables and linked lists, enabling fast search of which files contain a given word and its frequency.
